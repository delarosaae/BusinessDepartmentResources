/*
const showResultsSoFar = (events) =>{
    setShowDropDown(!showDropDown)
    const resultsList = searchDummy.map((result) => <li>{result.sentence}</li>)

    if(events.target.value.length > 2){

    }
}
*/
